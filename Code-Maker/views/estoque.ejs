
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Estoque</title>
    <!-- Correto -->
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
</head>

<body>

    <div class="container d-flex flex-wrap gap-3 py-4">
        <div class="row">
            <% estoque.forEach(item=> { %>
                <div class="col-md-4">
                    <div class="card">
                        <img style="height: 50vh" src="/images/<%= item.foto %>" class="card-img-top" alt="<%= item.nome %>">
                        <div class="card-body" style="height: 30%;">
                            <h5 class="card-title">
                                <%= item.nome %>
                            </h5>
                            <p class="card-text">
                                <%= item.descricao %>
                            </p>
                        </div>
                    </div>
                </div>
                <% }); %>
        </div>
    </div>

    <script src="/javascripts/bootstrap.min.js"></script>
</body>

</html>
